@model HairSalon.Models.Client

<section class="card">
  <div class="card-header">
    <h2 class="card-title">Create a new client</h2>
  </div>

  <div class="card-body">
    <form asp-action="Create">
      <div class="row mb-3">
        <label 
          asp-for="Id_Stylist" 
          class="col-sm-2 col-form-label"
        ></label>
        <select 
          asp-for="Id_Stylist" 
          asp-items="@ViewBag.Stylists"
          class="form-select"
          required
        ></select>
      </div>

      <div class="row mb-3">
        <label 
          asp-for="Name"
          class="col-sm-2 col-form-label"
        ></label>
        <input 
          asp-for="Name"
          class="form-control"
          required
        />
      </div>

      <button 
        type="submit"
        class="btn btn-primary"
      >Create Client</button>
    </form>
  </div>
</section>